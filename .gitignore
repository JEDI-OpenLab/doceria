# Dépendances et build
node_modules/
dist/

# Secrets — ne jamais committer la clé API
.env
.env.local
.env.*.local

# Documentation LOCALE privée (valeurs réelles : IP, jeton, mot de passe…) — ne JAMAIS committer
MOODLE-LOCAL.md
*-LOCAL.md
*.local.md

# Système
.DS_Store
*.log
npm-debug.log*
